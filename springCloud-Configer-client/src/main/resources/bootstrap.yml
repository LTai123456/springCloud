spring:
  application:
    name: configer-server                             #指定了配置文件的应用名
  cloud:
    config:
      uri: http://peer1:8086/        #Config server的uri
      profile: service                   #指定的环境
      label: master                      #指定分支
server:
  port: 8085


